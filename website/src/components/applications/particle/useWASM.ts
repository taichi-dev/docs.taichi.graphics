// import React, {useRef, useEffect, useState} from "react";
//
// export const useWASM = (fileName, imports) => {
//     const [loaded, setLoaded] = useState(null);
//     useEffect(() => {
//         const fetch WASM = async () => {
//             const wasm = await fetch(fileName);
//             const wasmInstance = await
//         }
//     })
// }